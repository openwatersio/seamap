{
  "name": "seamap-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "seed": "./seed.sh",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "test": "node --test src/*.test.mjs"
  },
  "dependencies": {
    "@openwaters/seamap": "*",
    "pmtiles": "^4.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250409.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.0.0"
  }
}
