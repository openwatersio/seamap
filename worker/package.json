{
  "name": "seamap-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "seed": "./seed.sh",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "test": "node --test src/*.test.mjs"
  },
  "dependencies": {
    "@openwaters/seamap": "*",
    "pmtiles": "^4.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260821.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
