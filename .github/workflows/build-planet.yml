name: Build planet & publish

# Boots an ephemeral Hetzner Cloud box, runs the full planet build, publishes
# PMTiles to R2, and always destroys the box. Compute is pay-per-build; this
# runner only orchestrates.
#
# Required repo secrets:
#   HCLOUD_TOKEN            Hetzner Cloud API token
#   R2_ACCESS_KEY_ID        R2 (S3) access key
#   R2_SECRET_ACCESS_KEY    R2 (S3) secret key

on:
  schedule:
    - cron: "0 3 * * 1" # Mondays 03:00 UTC
  workflow_dispatch:
    inputs:
      server_types:
        description: "Hetzner types to try in order; first with live capacity anywhere wins"
        default: "ccx43 ccx53"
      area:
        description: "Geofabrik area for a cheap smoke build, lowercase (blank = full planet)"
        default: ""
      location:
        description: "Restrict to one Hetzner location (blank = any)"
        default: ""

concurrency:
  group: build-planet
  cancel-in-progress: false

permissions:
  contents: read
  actions: write # dispatches the worker/viewer deploys after publishing

env:
  # Types are tried in order against live capacity (the datacenter API lists
  # which types are orderable right now); capacity moves around, so no location
  # is pinned. Every candidate needs ~280GB free disk — the planet build (~82
  # PBF + ~110 nodemap + ~25 output + scratch) runs on local NVMe with no
  # Hetzner volume, since the account's volume quota is taken by another project.
  SERVER_TYPES: ${{ inputs.server_types || 'ccx43 ccx53' }}
  AREA: ${{ inputs.area }}
  SERVER_IMAGE: ubuntu-24.04
  SERVER_LOCATION: ${{ inputs.location }}
  R2_ENDPOINT: https://7822da9c68cfce969e63d07534969359.r2.cloudflarestorage.com
  R2_BUCKET: tiles
  SSH_OPTS: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ConnectTimeout=10"
  NAME: seamap-build-${{ github.run_id }}

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HCLOUD_TOKEN: ${{ secrets.HCLOUD_TOKEN }}
    steps:
      - uses: actions/checkout@v7

      - name: Install hcloud CLI
        run: |
          curl -fsSL https://github.com/hetznercloud/cli/releases/latest/download/hcloud-linux-amd64.tar.gz \
            | tar xz hcloud
          sudo mv hcloud /usr/local/bin/

      # Print the valid type names for this account so we never guess them.
      - name: List server types
        run: hcloud server-type list -o columns=name,cores,memory,disk,cpu_type,architecture

      - name: Boot ephemeral server
        run: |
          ssh-keygen -t ed25519 -N "" -f id_build -q
          hcloud ssh-key create --name "$NAME" --public-key-from-file id_build.pub

          # Pick the first type from SERVER_TYPES with capacity in any datacenter
          # (server_types.available is live stock, unlike server-type list), then
          # still tolerate a create failure — stock can vanish between the query
          # and the create — by falling through to the next candidate.
          types_json=$(hcloud server-type list -o json)
          dcs_json=$(hcloud datacenter list -o json)
          created=""
          for T in $SERVER_TYPES; do
            tid=$(echo "$types_json" | jq -r --arg t "$T" '.[] | select(.name==$t) | .id')
            [ -n "$tid" ] || { echo "unknown server type: $T" >&2; continue; }
            for LOC in $(echo "$dcs_json" | jq -r --argjson id "$tid" \
                '.[] | select(.server_types.available | index($id)) | .location.name' | sort -u); do
              [ -n "$SERVER_LOCATION" ] && [ "$LOC" != "$SERVER_LOCATION" ] && continue
              echo "trying $T in $LOC"
              if hcloud server create --name "$NAME" \
                  --type "$T" --image "$SERVER_IMAGE" --location "$LOC" \
                  --ssh-key "$NAME"; then
                created=1
                break 2
              fi
            done
          done
          [ -n "$created" ] || { echo "no capacity anywhere for: $SERVER_TYPES" >&2; exit 1; }

          IP=$(hcloud server ip "$NAME")
          echo "IP=$IP" >> "$GITHUB_ENV"
          for i in $(seq 1 60); do
            ssh $SSH_OPTS -i id_build "root@$IP" true 2>/dev/null && exit 0
            sleep 5
          done
          echo "server never became reachable" >&2; exit 1

      - name: Build & publish
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          # Secrets/config to source on the box (kept off the command line).
          cat > build.env <<EOF
          export RCLONE_CONFIG_R2_TYPE=s3
          export RCLONE_CONFIG_R2_PROVIDER=Cloudflare
          export RCLONE_CONFIG_R2_ENDPOINT=$R2_ENDPOINT
          export RCLONE_CONFIG_R2_ACCESS_KEY_ID=$R2_ACCESS_KEY_ID
          export RCLONE_CONFIG_R2_SECRET_ACCESS_KEY=$R2_SECRET_ACCESS_KEY
          export R2_BUCKET=$R2_BUCKET
          export AREA=$AREA
          export TMP_DIR=/root/tmp
          EOF

          rsync -az --exclude .git --exclude node_modules --exclude data \
            -e "ssh $SSH_OPTS -i id_build" ./ "root@$IP:/root/seamap/"
          scp $SSH_OPTS -i id_build build.env "root@$IP:/root/seamap/build.env"

          ssh $SSH_OPTS -i id_build "root@$IP" bash -se <<'REMOTE'
          set -euo pipefail
          export DEBIAN_FRONTEND=noninteractive
          apt-get update -qq
          apt-get install -y -qq openjdk-21-jdk-headless rclone unzip curl
          cd /root/seamap
          set -a; source build.env; set +a
          # Everything (PBF, nodemap, output) lives on the local NVMe; the server
          # type's disk is sized for it. Fail before building if that's not true.
          if [ -z "${AREA:-}" ]; then
            AVAIL_GB=$(df -BG --output=avail / | tail -1 | tr -dc 0-9)
            [ "$AVAIL_GB" -ge 280 ] || { echo "need ~280GB free for a planet build, have ${AVAIL_GB}GB — use a bigger server type" >&2; exit 1; }
          fi
          mkdir -p data "$TMP_DIR"
          # Pre-fetch land polygons with retries (the build's own downloader has none).
          if [ ! -f data/land-polygons-split-4326/land_polygons.shp ]; then
            curl -fSL --retry 5 --retry-all-errors --connect-timeout 30 \
              -o /tmp/land.zip \
              https://osmdata.openstreetmap.de/download/land-polygons-split-4326.zip
            unzip -q /tmp/land.zip -d data
          fi
          if [ -n "${AREA:-}" ]; then
            # Smoke build: small regional extract, do not touch the `latest` pointer.
            AREA="$(echo "$AREA" | tr '[:upper:]' '[:lower:]')"
            JAVA_OPTS="-Xmx8g" bin/run --area="$AREA" --force
            VERSION="smoke-$AREA" UPDATE_LATEST=false bin/publish
          else
            bin/build-planet
          fi
          REMOTE

      # Worker and viewer deploy only here (plus manual dispatch), so style
      # changes ship together with the tiles that carry the schema they expect.
      # Smoke builds don't flip `latest`, so they don't deploy anything.
      - name: Dispatch worker & viewer deploys
        if: env.AREA == ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh workflow run deploy-worker.yml --ref main
          gh workflow run pages.yml --ref main

      - name: Destroy server
        if: always()
        run: |
          hcloud server delete "$NAME" || true
          hcloud ssh-key delete "$NAME" || true
