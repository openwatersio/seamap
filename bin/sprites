#!/usr/bin/env bash
# Chart symbols live in the @openwaters/seamap package; the viewer copies its
# built sheet at vite build/dev time (viewer/vite.config.js).
set -euo pipefail
exec "$(dirname "$0")/../style/bin/sprites"
