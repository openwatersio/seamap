#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

# npm first: bin/sprites composes POI badges from the @iconify-json packages.
npm install
bin/sprites

# Resolve dependencies, compile, test.
exec ./gradlew build
